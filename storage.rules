rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Event images: only authenticated users can upload; path must be events/{userId}/...
    match /events/{userId}/{fileName} {
      allow read: if true;
      allow write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
